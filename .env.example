# ============================================================
# Environment Variables Configuration
# ============================================================
# This file is auto-loaded by the application if present.
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Load order (later overrides earlier):
# 1. Default values (in code)
# 2. .env file (this file) - won't override existing env vars
# 3. .env.local file (for local overrides, gitignored)
# 4. Process environment variables (export VAR=value)
# 5. YAML config file with ${VAR} expansion
# ============================================================

# Application
APP_NAME=go-alert-web3-bnb
APP_ENV=development

# WebSocket
WS_URL=wss://nbstream.binance.com/w3w/stream
WS_RECONNECT_INTERVAL=5s
WS_MAX_RETRIES=10

# Telegram (required for notifications)
# Get bot token from @BotFather on Telegram
# Get chat_id from @userinfobot or API: https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
TELEGRAM_RATE_LIMIT=30

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_POOL_SIZE=10
REDIS_LOCK_TTL=30s
REDIS_PROCESSED_TTL=5m

# Logger
LOG_LEVEL=info
LOG_FORMAT=json
LOG_OUTPUT=stdout

# Tokens (optional - can also be configured in config.yaml)
# Format: address:chain_type:streams (semicolon-separated for multiple)
# Chain types: CT_501 (Solana), 56 (BSC), 8453 (Base)
# Streams: ticker24h, holders, tx, kline
# Example: TOKENS=addr1:CT_501:ticker24h,holders;addr2:56:ticker24h
